<%= form_for(phone_request) do |f| %>
  <% if phone_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phone_request.errors.count, "error") %> prohibited this phone_request from being saved:</h2>

      <ul>
      <% phone_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
